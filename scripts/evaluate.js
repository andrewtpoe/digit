// const { loadData } = require('./load-data');
// const { initializeNetwork } = require('../src');

(async () => {
  console.log("TODO: Create the evaluate script.");
  // const { testData } = await loadData;

  // const network = initializeNetwork();

  // network.evaluateModel(testData, 'default');
})();
